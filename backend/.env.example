# Configuration Backend FaceWork
# Copier ce fichier vers .env et adapter selon l'environnement

# === Serveur Express ===
PORT=5001
NODE_ENV=development

# === Base de données MongoDB ===
# Développement local
MONGODB_URI=mongodb://127.0.0.1:27017/facework

# Production (MongoDB Atlas - créer sur mongodb.com/cloud/atlas)
# Remplacer par votre vraie URL de connexion
MONGODB_URI_PROD=mongodb+srv://username:password@cluster.mongodb.net/facework?retryWrites=true&w=majority

# === Sécurité JWT ===
# Générer une clé secrète forte (32+ caractères)
# Commande : node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=votre_cle_secrete_jwt_32_caracteres_minimum
JWT_EXPIRES_IN=7d

# === CORS Frontend ===
# Développement
FRONTEND_URL=http://localhost:5173

# Production Vercel
# FRONTEND_URL=https://facework.vercel.app

# === Email (optionnel) ===
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
