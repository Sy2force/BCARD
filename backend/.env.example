# Configuration serveur backend FaceWork
# Copier ce fichier vers .env et remplir les valeurs

# === Configuration Serveur ===
PORT=5001
NODE_ENV=development  # development | production

# === Base de données MongoDB ===
# Local : MongoDB installé sur votre machine
MONGODB_URI=mongodb://127.0.0.1:27017/facework

# Production : MongoDB Atlas (créer un compte gratuit sur mongodb.com/cloud/atlas)
MONGODB_URI_PROD=mongodb+srv://username:password@cluster.mongodb.net/facework?retryWrites=true&w=majority

# === Sécurité JWT ===
# Générer une clé secrète forte (minimum 32 caractères)
# Astuce : node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=changez_cette_cle_secrete_par_une_vraie_cle_aleatoire
JWT_EXPIRES_IN=7d

# === Frontend CORS ===
# URL du frontend en développement local
FRONTEND_URL=http://localhost:5173
# Production : remplacer par https://facework.vercel.app

# === Email (optionnel) ===
# Pour l'envoi d'emails (récupération de mot de passe, etc.)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
